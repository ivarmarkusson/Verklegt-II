@model Mooshak2.Models.ViewModels.AssignmentViewModel

@{
    ViewBag.Title = "AddMilestone";
}

@using (Html.BeginForm("AddMilestone", "Teacher", FormMethod.Post))
{
    <legend>Add Milestone</legend>
    
    <input type="hidden" class="form-control" name="AssignmentID" value="@Model.ID">

    <div class="form-group">
        <label>
            Milestone Title
            <input type="text" class="form-control" name="title">
        </label>
    </div>

    <div class="form-group">
        <label>
            Percentage
            <input type="text" class="form-control" name="percentage">
        </label>
    </div>

    

    <input type="submit" class="btn btn-info" value="Add Milestone">
}